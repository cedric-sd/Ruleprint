{
  "name": "@ruleprint/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure rule engine: merge, precedence, fingerprint and diff. No fs, no process, no network.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
