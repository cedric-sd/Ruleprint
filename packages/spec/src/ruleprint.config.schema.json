{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ruleprint.dev/schema/config-v0.json",
  "title": "RulePrintConfig",
  "description": "Contents of .ruleprint/config.json: opt-in collectors and their settings.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ast": { "$ref": "#/definitions/AstConfig" }
  },
  "definitions": {
    "AstConfig": {
      "title": "AstConfig",
      "description": "Settings of the AST collector (ADR-0007). Present means enabled.",
      "type": "object",
      "additionalProperties": false,
      "required": ["include"],
      "properties": {
        "include": {
          "description": "Globs, relative to the scanned root, of the files to analyse (e.g. src/domain/**).",
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "exclude": {
          "description": "Globs of files to leave out even when included.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "glossary": {
          "description": "Domain terms; an identifier containing one of them counts as a domain signal.",
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
